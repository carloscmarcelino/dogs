import { extendTheme } from '@chakra-ui/react';

import '@fontsource/montserrat';

export const theme = extendTheme({
  colors: {
    orange: '#e80',
    purple1: '#87f',
    purple2: '#76f',
    white: '#fff',
  },
  fonts: {
    body: 'montserrat',
  },
});
